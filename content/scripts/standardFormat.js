var innerFrame = document.getElementsByTagName("iframe");

if (innerFrame && innerFrame.length > 0) {
  innerFrame = innerFrame[0];

  if (/bibliovation\.com\/cgi-bin\/koha\/members\/memberentry\.pl/.test(innerFrame.src)) {

  }
}
